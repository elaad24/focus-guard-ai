# focus-guard-ai runs entirely locally.
# No environment variables are required for MVP.
# Optional overrides (not used by default):
# FOCUS_GUARD_CAMERA_SOURCE=browser
# Use "opencv" only if you want the Python backend to open the webcam directly.
# FOCUS_GUARD_PORT=8787
# VITE_FOCUS_GUARD_PORT=8787
